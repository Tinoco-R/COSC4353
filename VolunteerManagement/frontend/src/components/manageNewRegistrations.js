import React, { Component } from "react";

export default class ManageNewRegistrations extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (<p>This is where you manage new registrations</p>);
    }
}