import React, { Component } from "react";

export default class ViewVolunteers extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (<p>These are all the volunteers on the site</p>);
    }
}