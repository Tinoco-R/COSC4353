import React, { Component } from "react";

export default class AllEventsAdmin extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (<p>These are all the events on the site</p>);
    }
}